package padej.soup.module.modules;
import padej.soup.module.Module;
import padej.soup.module.ModuleCategory;
public final class SprintLockModule extends Module {
    public SprintLockModule(){ super("SprintLock", "Keeps sprint active while moving forward.", ModuleCategory.PLAYER); }
}
